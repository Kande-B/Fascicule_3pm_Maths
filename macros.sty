\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{macros}[2026/08/19 v1.0 Macros et Raccourcis Maths-Sciences]

% ==============================================================================
% 1. ENSEMBLES ET SYMBOLES MATHÉMATIQUES
% ==============================================================================
\newcommand{\N}{\mathbb{N}}
\newcommand{\Z}{\mathbb{Z}}
\newcommand{\D}{\mathbb{D}}
\newcommand{\Q}{\mathbb{Q}}
\newcommand{\R}{\mathbb{R}}
\newcommand{\C}{\mathbb{C}}

\newcommand{\intervalle}[2]{[#1\,;\,#2]}
\newcommand{\intervallefo}[2]{[#1\,;\,#2[}
\newcommand{\intervalleof}[2]{]#1\,;\,#2]}
\newcommand{\intervalleoo}[2]{]#1\,;\,#2[}

% Notations des vecteurs
\newcommand{\vect}[1]{\overrightarrow{#1}}
\newcommand{\vcoords}[2]{\begin{pmatrix} #1 \\ #2 \end{pmatrix}}
\newcommand{\vcoordsz}[3]{\begin{pmatrix} #1 \\ #2 \\ #3 \end{pmatrix}}

% Probabilités
\renewcommand{\Pr}{\mathbb{P}}
\newcommand{\Proba}[1]{\Pr\left(#1\right)}
\newcommand{\ProbaCond}[2]{\Pr_{#2}\left(#1\right)}

% ==============================================================================
% 2. SYMBOLES D'OPÉRATIONS ET D'ÉVALUATION
% ==============================================================================
\newcommand{\x}{\times}
\newcommand{\ssi}{\Longleftrightarrow}
\newcommand{\donc}{\Longrightarrow}
\newcommand{\equivalent}{\iff}

% Degré et unités fréquentes
\newcommand{\degre}{^{\circ}}
\newcommand{\euro}{\text{ \euro}}

% Compétences du référentiel (Grille nationale Bac Pro / DNB Pro)
\newcommand{\capSApproprier}{\badge[colPrimary]{C1 $\cdot$ S'approprier}}
\newcommand{\capAnalyser}{\badge[colSecondary]{C2 $\cdot$ Analyser/Raisonner}}
\newcommand{\capRealiser}{\badge[colProp]{C3 $\cdot$ Réaliser}}
\newcommand{\capValider}{\badge[colExem]{C4 $\cdot$ Valider}}
\newcommand{\capCommuniquer}{\badge[colMeth]{C5 $\cdot$ Communiquer}}

% ==============================================================================
% 3. DESSIN ET GRAPHES RAPIDES TIKZ
% ==============================================================================
% Repère orthonormé standard
\newcommand{\repereOrtho}[4]{% [xmin, xmax, ymin, ymax]
    \draw[step=1cm, slate200, very thin] (#1,#3) grid (#2,#4);
    \draw[thick, ->, >=stealth, slate700] (#1,0) -- (#2,0) node[right] {\footnotesize $x$};
    \draw[thick, ->, >=stealth, slate700] (0,#3) -- (0,#4) node[above] {\footnotesize $y$};
    \node[below left, font=\footnotesize] at (0,0) {$O$};
    \draw[thick] (1,0.1) -- (1,-0.1) node[below, font=\footnotesize] {$1$};
    \draw[thick] (0.1,1) -- (-0.1,1) node[left, font=\footnotesize] {$1$};
}

\endinput
